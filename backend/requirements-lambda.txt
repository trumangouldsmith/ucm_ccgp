# Lambda deployment requirements (minimal)
# This is what gets packaged for Lambda deployment
# pandas/numpy/boto3 provided by AWS Lambda Layer

fastapi==0.104.1
mangum==0.17.0
pydantic==2.5.0
yfinance==0.2.32

